# PubMed searches

This folder contains exported PubMed search result CSVs used as inputs to the notebook.

Currently this folder includes `search_*.csv` exports (e.g., `search_01.csv` through `search_028.csv`).
